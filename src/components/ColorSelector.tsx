import React from 'react';

const ColorSelector: React.FC = ({colors}: any) => {
  return (
    <div>
        <button onClick={()=>console.log('Hola')} >Click</button>
    </div>
  );
};

export default ColorSelector;